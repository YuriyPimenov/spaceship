import React, {FC} from 'react';
import styles from './menu.module.scss';

const Menu: FC = () => {
    return (
        <div className={styles.root}>
            Menu
        </div>
    );
};

export {Menu};